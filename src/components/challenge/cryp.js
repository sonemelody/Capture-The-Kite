import styled from "styled-components";

const CrypSection = styled.section`
  font-family: Pretendard-Bold;
  .problemList {
    list-style-type: none;
  }
`;

const Cryp = () => {
  return <CrypSection></CrypSection>;
};

export default Cryp;
